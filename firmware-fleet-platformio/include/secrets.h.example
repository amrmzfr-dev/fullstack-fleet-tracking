#pragma once

// Device credentials — copy this file to secrets.h and fill in real values.
// secrets.h is gitignored; never commit it.
#define API_KEY "change-me-api-key"
#define BACKEND_HOST "your-backend.example.com"
